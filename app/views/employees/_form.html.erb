<%= form_for @company_employee, url: locals[:url] do |f| %>
  <% if @company_employee.errors.any? %>
    <div id="error_explanation">
      <h2>
        <%= pluralize(@company_employee.errors.count, "error") %> prohibited
        this employee from being saved:
      </h2>
      <ul>
        <% @company_employee.errors.full_messages.each do |msg| %>
          <li><%= msg %></li>
        <% end %>
      </ul>
    </div>
  <% end %>
  <div>
    <%= f.label :name %>
    <%= f.text_field :name %>
  </div><br />
  <div>
    <%= f.label :age %>
    <%= f.text_field :age, maxlength: 2 %>
  </div><br />
  <div>
    <%= f.label :email %>
    <%= f.text_field :email %>
  </div><br />
  <div>
    <%= f.label "Company" %>
	<%= f.collection_select(:company_id, @companies, :id, :company_name) %>
  </div><br />
  <div>
    <%= f.submit %>
  </div>
  <p>
  	<%= link_to "Back", company_employees_path %>
  </p>
<% end %>